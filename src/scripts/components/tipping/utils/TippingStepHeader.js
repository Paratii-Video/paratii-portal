/* @flow */

import styled from 'styled-components'

import Colors from 'components/foundations/base/Colors'

const TippingStepHeader = styled.div`
  color: ${Colors.white};
  font-weight: bold;
  font-size: 25px;
`

export default TippingStepHeader
